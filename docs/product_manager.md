# Touchline Product Management Strategy & Handoff Document

## Executive Product Mandate

Touchline is a browser-based, asynchronous, persistent multiplayer football-management universe. The product prioritizes social competition, persistent consequences, explainable systems, meaningful financial trade-offs, and enduring world history.

The primary product thesis is:
> **Every club has a personality. Every player has a story. Every decision has consequences.**

The MVP core validation question (PRD §85) is:
> **"Is managing a club in a world populated by other real managers more compelling than managing a club alone?"**

The PRD (`docs/Touchline — Persistent Multiplayer Football Manager PRD.md`) and Technical Plan (`docs/Touchline_Technical_Implementation_Plan.md`) serve as the authoritative sources of truth. `OPENCODE.md` records resolved architectural decisions and domain invariants. This document acts as the product strategy manifest, sprint breakdown directory, open-decision registry, and team handoff guide.

---

## Delivery Posture & Technical Invariants

- **Strict Phasing:** Ship scope strictly according to the task manifests in `docs/tasks/`. Do not pull V1+ features into Phase 1 MVP merely because schema hooks exist.
- **Server Authority:** The server is the sole source of truth. The Nuxt 3 client renders state and submits commands; it never simulates outcomes (OPENCODE.md, Tech Plan §1).
- **Append-Only Ledger:** Financial balance is strictly `SUM(ledger_entries)`. No stored `balance` column exists in `finance` tables (OPENCODE.md, Tech Plan §6).
- **Event Spine:** All state changes emit typed `world.events` carrying `world_id`, tick counters, actor IDs, causal chains (`CausedBy`), and typed JSON payloads (Tech Plan §4).
- **Explainability:** All scored outcomes (board confidence, transfer desire, job security) return `Explanation` objects with explicit factor breakdowns (Tech Plan §8).
- **Relationship Graph:** Modeling entity relationships as polymorphic graph edges in `social.relationships` (`player`, `manager`, `club`) (Tech Plan §6).
- **Unified PolicyBot Execution:** Absence delegation and AI-controlled clubs invoke the exact same command handlers a human manager calls (Tech Plan §10).
- **Competitive Fairness:** Strict non-pay-to-win policy. Real-money purchases cannot grant competitive or financial simulation advantages (PRD §78).

---

## Complete Application Sprint & Task Breakdown

The delivery roadmap translates the PRD and Technical Implementation Plan into 18 structured sprints (`S00` through `S17`) across 5 major development phases:

### Phase 0 — Foundations & Control Plane (Pre-MVP)
*Goal: Establish core engine scaffolding, event bus, player generation, auth, and world tick scheduler.*

- **Sprint 00 — Product control plane**
  - [`S00-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S00-01-product-delivery-governance.md): Establish product delivery governance.
- **Sprint 01 — World foundation and event spine**
  - [`S01-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S01-01-core-migrations-and-world-scoping.md): Create core schemas and world-scoped migrations.
  - [`S01-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S01-02-event-log-and-river-round-trip.md): Wire `world.events` and river event-bus queue.
  - [`S01-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S01-03-explanation-contract.md): Implement standard `Explanation` object contracts.
  - [`S01-04`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S01-04-player-generation-data-and-seeding.md): Curate `pkg/playergen` name datasets and weighted nationality pool.
  - [`S01-05`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S01-05-local-development-and-environment-contract.md): Establish Neon Postgres and local dev environment contracts.
- **Sprint 02 — Authenticated, schedulable worlds**
  - [`S02-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S02-01-gin-api-auth-and-session-flow.md): Build Gin REST router, JWT authentication, and session flows.
  - [`S02-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S02-02-world-lifecycle-and-club-assignment-contract.md): Implement world lifecycle and single-club manager assignment rules.
  - [`S02-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S02-03-configurable-world-clock.md): Build configurable world tick scheduler emitting `WORLD_TICK` events.
  - [`S02-04`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S02-04-redis-and-single-websocket-foundation.md): Implement Redis session store and single multiplexed WebSocket foundation (`useSocket`).
- **Sprint 03 — Seeded playable world bootstrap**
  - [`S03-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S03-01-world-bootstrap-and-first-squad.md): Produce seeded world, club, and procedurally generated first squad.
  - [`S03-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S03-02-phase-zero-vertical-slice.md): Validate Phase-0 vertical slice end-to-end.

---

### Phase 1 — MVP Core Loop
*Goal: Deliver playable persistent multiplayer competition, transfer market, board consequences, and dashboard.*

- **Sprint 04 — Deterministic football competition**
  - [`S04-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S04-01-competition-fixtures-and-standings.md): Implement competition scheduling, fixtures, and standings.
  - [`S04-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S04-02-deterministic-match-engine.md): Build pure deterministic match simulation engine (`Simulate`).
  - [`S04-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S04-03-mvp-matchday-event-feed.md): Build text/event-feed matchday commentary viewer over WebSockets.
- **Sprint 05 — Manager controls and finance**
  - [`S05-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S05-01-squad-tactics-and-simple-training.md): Implement squad selection, tactics management, and Simple training mode.
  - [`S05-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S05-02-ledger-finance-and-contract-wages.md): Implement append-only ledger finance, cash vs budget, and wage commitments.
- **Sprint 06 — Multiplayer market and board consequences**
  - [`S06-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S06-01-transfers-listings-bids-and-negotiation.md): Build transfer engine, listings, cash/clause bids, and human negotiation workflow.
  - [`S06-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S06-02-board-confidence-mandates-and-sackings.md): Implement structured board mandates, confidence scoring, and firing events.
  - [`S06-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S06-03-player-morale-playing-time-and-transfer-requests.md): Model player morale, playing time promises, and transfer requests.
  - [`S06-04`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S06-04-social-messaging-manager-profiles-and-rivalries.md): Build manager profiles, direct messaging, and relationship graph foundations.
  - [`S06-05`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S06-05-absence-mode-and-policybot.md): Implement policy-driven absence delegation and PolicyBot fallback execution.
- **Sprint 07 — MVP experience, operations, and validation**
  - [`S07-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S07-01-home-dashboard-aggregator.md): Build `GET /api/dashboard` aggregator and Nuxt 3 dashboard UI.
  - [`S07-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S07-02-email-notifications-and-preferences.md): Build notification preferences and transactional email worker.
  - [`S07-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S07-03-pwa-shell-and-offline-resilience.md): Configure `@vite-pwa/nuxt` module, service worker, and offline shell caching.
  - [`S07-04`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S07-04-mvp-e2e-validation-and-load-testing.md): Execute E2E flow validation and k6 load testing suite.

---

### Phase 2 — V1 Depth & Integrity
*Goal: Introduce youth academies, rich player personalities, squad dynamics, news generation, agents, and anti-abuse.*

- **Sprint 08 — Academies and player development**
  - [`S08-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S08-01-academy-investment-and-procedural-intake.md): Implement academy investment tiers and procedural youth intake.
  - [`S08-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S08-02-dynamic-potential-and-player-development.md): Implement dynamic potential growth and age-curve player development.
  - [`S08-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S08-03-injuries-medical-staff-and-recovery.md): Implement injury simulation, medical staff quality, and rehabilitation.
- **Sprint 09 — Relationship-driven football world**
  - [`S09-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S09-01-full-player-personality-and-hidden-traits.md): Implement full player personality archetype system and hidden traits.
  - [`S09-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S09-02-dressing-room-factions-and-squad-dynamics.md): Implement graph-derived squad dynamics and dressing room factions.
  - [`S09-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S09-03-news-generator-and-media-read-model.md): Build automated news generator and media read-model over event log.
- **Sprint 10 — Agents, competitions, identity, and ownership**
  - [`S10-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S10-01-agents-and-semi-autonomous-negotiations.md): Implement player agents and semi-autonomous contract negotiations.
  - [`S10-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S10-02-manager-created-competitions-and-reputation.md): Implement manager-created competitions and reputation growth.
  - [`S10-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S10-03-club-dna-and-supporters.md): Implement club DNA archetypes and supporter group mechanics.
  - [`S10-04`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S10-04-anti-abuse-trade-monitoring-and-commissioner-tooling.md): Implement anti-abuse trade monitoring and commissioner adjudication tooling.
- **Sprint 11 — V1 integrity and launch review**
  - [`S11-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S11-01-v1-golden-replay-and-integrity-verification.md): Build V1 golden replay test suites and financial ledger property tests.
  - [`S11-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S11-02-v1-production-deployment-and-launch.md): Finalize production Helm charts, k3s Contabo cluster, and V1 launch.

---

### Phase 3 — V2 Ecosystem & Scale
*Goal: Enable club creation, manager ownership path, national teams, retired player careers, and gRPC service extraction.*

- **Sprint 12 — Club creation and ownership path**
  - [`S12-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S12-01-club-creation-flow-and-league-placement.md): Implement club creation flow, financial backing validation, and lower-tier placement.
  - [`S12-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S12-02-manager-ownership-and-chairman-transition.md): Implement manager-to-owner transition and chairman authority mechanics.
- **Sprint 13 — International world and careers**
  - [`S13-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S13-01-national-team-football-and-international-competitions.md): Implement national team football, international call-ups, and tournaments.
  - [`S13-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S13-02-person-entity-and-retired-player-careers.md): Refactor player/manager identity into Person entity for retired player careers.
  - [`S13-03`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S13-03-stadium-development-and-advanced-sponsorship.md): Implement stadium expansion projects and commercial sponsorship deals.
- **Sprint 14 — V2 infrastructure and world expansion**
  - [`S14-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S14-01-grpc-inter-engine-extraction.md): Extract Match and Transfer engines into standalone gRPC microservices.
  - [`S14-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S14-02-multi-world-scaling-and-world-matchmaking.md): Implement multi-world matchmaking, cross-world reputation, and world scaling.

---

### Phase 4 — V3 Inhabitable Universe
*Goal: Unlock multi-role gameplay (scouts, journalists, agents, directors), global economy, and fair monetization.*

- **Sprint 15 — Multi-role universe foundation**
  - [`S15-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S15-01-multi-role-actor-framework.md): Implement multi-role actor framework for inhabitable universe roles.
  - [`S15-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S15-02-scout-and-journalist-inhabitable-roles.md): Implement specialized gameplay interfaces for Scout and Journalist roles.
- **Sprint 16 — Global economy and fair monetization**
  - [`S16-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S16-01-global-football-economy-simulation.md): Implement global macro-economic simulation and TV rights distribution.
  - [`S16-02`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S16-02-cosmetic-and-analytics-monetization.md): Implement premium analytics suite, cosmetic customizations, and fairness verification.
- **Sprint 17 — V3 launch and ongoing governance**
  - [`S17-01`](file:///Users/bfree/Desktop/booluw/touchline/docs/tasks/S17-01-v3-universe-launch-and-ongoing-governance.md): Execute V3 universe launch and establish ongoing community governance.

---

## Open Product Decisions Registry

These decisions are deliberately open in the source specifications. Engineering teams must consume configuration parameters rather than hardcoding arbitrary assumptions. The PM must resolve these with stakeholders as sprint dependencies approach:

| Decision ID | Open Topic | Impacted Sprints | Governing Resolution Standard |
|---|---|---|---|
| **OPD-02** | Registration & Account Policy | S02-01 | Define age/privacy requirements, identity verification thresholds, and password reset flows. **⚠ ESCALATED (S03-02):** the Phase-0 vertical slice is verified end to end only via the admin bootstrap + dev-only `cmd/user-create` (OPD-15(6)). There is **no public onboarding flow** — a self-service "create account / start playing" path (registration, age/privacy consent, password reset) is **blocked** on this decision before any product launch. |
| **OPD-03** | Simulation Formulae & Tuning | S04, S05, S06 | Standardize mathematical weights for board job security (resolved — board slice at S06-02), injury probability curves, and morale decay rates (resolved — morale slice at S06-03). |
| **OPD-06** | Messaging Moderation Policy | S06-04 | Establish reporting/blocking mechanics, text moderation filters, and commissioner ban policies. |
| **OPD-07** | Email Provider & Consent | S07-02 | Select transactional provider (SES/Postmark/SendGrid), sender domain identity, and GDPR consent policy. |
| **OPD-08** | Operational SLA Targets | S07-04, S11-02 | Set P95 API response thresholds (<200ms), DB failover objectives, and cluster autoscaling parameters. |
| **OPD-09** | V1/V2 Ownership Rules | S10, S12, S13 | Define minimum manager reputation required to create custom competitions or purchase club equity. |
| **OPD-10** | Monetization Packaging | S16-02 | Pricing tiers for premium analytical dashboards and cosmetic cosmetics, enforcing 0% pay-to-win. |

### Resolved OPD entries

| Decision ID | Resolution | Resolved by |
|---|---|---|
| **OPD-11** (topics doc `world.nationality_pool`) | The weighted nationality distribution pool lives in the `ref` schema — `ref.nationalities.generation_weight` plus `ref.name_pool` — not `world.nationality_pool`. Nationality/name data is identical across parallel worlds, so it is non-world-scoped by design (migrations `0002_ref`, `backend/migrations/README.md`). Tech plan §7 and the Phase-0 handoff notes used the name `world.nationality_pool`; that wording is superseded. `pkg/playergen` consumes the same weights regardless of table name (S01-04). | S01-01 |
| **OPD-12** (technical plan §8 / PRD §54 explainability contract) | The explanation contract lives in `pkg/explanation` (cross-cutting, not `internal/world`). The wire shape is pinned as `{"subject","score","factors":[{"label","delta"}]}` with all fields always emitted; additive-only changes. `Factors` are the authoritative reason and are **not** required to sum to `Score` (narrative "why" cases such as an AI club's bid carry no numeric total); an opt-in `Validate()` reports a summed-deltas mismatch only when a producer calls it. Explanations persist with their causing event in `world.events.explanation` (JSONB); consumers render stored reasons (reference `Render()` in the package) and never recalculate. State-changing API endpoints (S02+) must return the `Explanation` directly. | S01-03 |
| **OPD-13** (S01-04 data home, coverage, and player-persistence timing) | (**1) Data home + ingestion:** curated `data/names/<code>.json` files are the versioned, license-attributed record of truth; `cmd/ref-seed` ingests them idempotently into `ref.nationalities` + `ref.name_pool` (upsert nationalities, delete+reinsert name pool per code, one transaction; compose `ref-seed` service after migrations; CI runs it twice asserting identical output). Runtime squad generation reads the DB pools; `pkg/playergen` stays DB-free. (**2) Coverage:** 21 nationalities (README's 15 + colombia, uruguay, ghana, croatia, scotland, ivory_coast) with `generation_weight` values per the README table — tuning is data, not code. **Deferred:** `attribute_bias` and single-name/`shirt_name` rules (no `shirt_name` column yet). (**3) Codes:** lowercase 2–3 letter slugs; `eng`/`sco` are project-reserved non-ISO codes for the football home nations (documented in `ref.nationalities.code` guidance and `data/names/README.md`). (**4) Player-persistence timing — product decision:** players are **not** persisted by S01-04. Generated players (`GeneratedPlayer`) are written as `person.people` + `player.players` rows only when a team is created or the first season starts (fresh-slate worlds; S02-02/S03-01), so S01-04 touches reference data only and never game entities. | S01-04 |
| **OPD-03** (simulation formulae & tuning — board job-security slice, S06-02) | The board job-security formula and mandate/sacking numbers are standardized in `docs/design/board-numerics.md`, expressed as **proposal** constants in `internal/board` (recalibration is a data-only change). Per league: a human-managed club whose weighted board-confidence total falls ≤ **25** is sacked by the engine at weekly review (mandate fragility: league finish, points target, operating balance, wage structure). Confidence = weighted sum of seven 0–100 lens scores (performance, expected-finish vs position; football-points progress; wage structure; operating balance; club-DNA alignment — resolved for MVP as an ambition/patience clamp, richer alignment deferred to S10-03; supporter sentiment EWMA; alternatives with reputation slider). The morale curve was deferred to the S06-03 row below. | S06-02 |
| **OPD-03** (simulation formulae & tuning — morale slice, S06-03) | The S06-03 morale/playing-time/transfer-request numbers are standardized in `docs/design/morale-numerics.md`, expressed as **proposal** constants in `internal/player/numerics.go` (recalibration is a data-only change). Whole-season share = player minutes ÷ (90 × club completed matches), clamped to 1. Role entitlements: key_player 0.75 / rotation 0.45 / squad_player 0.20 / development 0. Every completed match pulls morale alpha-weighted toward a band target (satisfied 0.65 / neutral 0.50 / unhappy 0.35; personality steps: ambition & ego +0.08 tougher, patience & loyalty +0.06 gentler); swing alpha 0.35 scaled 0.5–1.5× by (professionalism, emotional_volatility). Weekly recovery toward neutral 0.5 at 0.10 × 0.5–2.0× by professionalism. Unhappiness becomes a formal transfer request only when morale ≤ 0.35 after recovery AND share < 0.5× expected, at a human-managed club with no open request and cooldown lapsed; approve lists the player at market value, deny drops morale 0.10 + 28-day cooldown, reassure files a 4-week playing-time promise (kept +10 / broken −30). Fresh start on transfer: morale 0.85, share 0. Internal match modifiers from morale are deferred until the performance model lands (S09-01) — S06-03 feeds the pitch, it never scores it. The remaining OPD-03 curve — injury probability (S08-03) — stays open under the registry row above. | S06-03 |
| **OPD-03** (simulation formulae & tuning — social trust slice, S06-04a) | A manager's **trust score** is the on-the-fly `SUM(delta)` of their append-only `social.trust_events` log — no materialized score (recalibration is instantly reflected across history). Migration 0041 seeds the day-one baseline by mirroring the S06-03 player↔manager journal (`transfer approved` +15 / `denied` −25 / promise `kept` +10 / `broken` −30; 0-delta reassures skipped), so every manager has history on profile launch. Messaging limits (2,000 chars, HTML tag strip, 30/min + 200/day per sender) and rivalry accumulation (club↔club always; manager↔manager only when both decks are human-managed; win/loss trust ±5 for human sides) are standardized in `docs/design/social-numerics.md`, expressed as **proposal** constants in `internal/social` (recalibration a data-only change). | S06-04 |
| **OPD-14** (developer-environment contract) | **Postgres is external by default:** the base `docker-compose.yml` defines no Postgres service — it runs on Neon and the connection string is brought by the developer (`DATABASE_URL`). A hermetic Postgres is an **opt-in override** (`infra/ci/docker-compose.postgres.yml`, postgres:16) used by CI's `compose` job and available locally. **Env contract:** a repo-root `.env` (template `.env.example`) drives compose; the merged values (`DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, TTLs, ports, `ENV`, `NUXT_PUBLIC_API_BASE`) are mirrored in `backend/.env.example` for bare `go run`/migrate runs — the two templates are kept in sync. Neon gotchas codified: unpooled endpoint (strip `-pooler`), scheme `postgres://` (golang-migrate/river reject `postgresql://`), strip surrounding quotes. **Health contract:** every backend binary fails fast with an actionable error when `DATABASE_URL` is missing/unreachable and exposes an HTTP liveness check — api `:8080/health` + `:8080/health/db` (pings Postgres, 503 when down), scheduler `:8081/health`, worker `:8082/health` — used by compose healthchecks and the CI smoke test. **Bootstrap order:** migrations → ref-seed → api/scheduler/worker → frontend (compose `depends_on`, one-shots use `service_completed_successfully`). **CI verification:** the `compose` job validates `docker compose config -q` with placeholder env and boots the full stack against the Postgres override, polling `/health/db` (up to ~10 min) then asserting frontend on :3000. Walkthrough: `docs/development.md`. | S01-05 |
| **OPD-15** (accounts, worlds, jobs, and sessions) | **(1) One account ⇒ many worlds:** `auth.users` is account-level; membership is one `manager.managers` row per (user, world). `uq_managers_user_world` enforces at most one row per user per world. **(2) One job per user, not per world:** a user holds at most one global job (`uq_manager_one_job_per_user` — partial unique index where `status='active' AND current_club_id IS NOT NULL`). No multi-job tie-break scenario exists. **(3) JWTs carry no `world_id`:** access token claims are `sub`=manager, `user_id`, `jti`, `exp`, `iat`, `type=access`; refresh is `sub`=manager, `jti`, `exp`, `iat`, `type=refresh`. The active world is *derived from the manager row* by manager id, never minted into a token (a `jti` also guarantees every token is unique so rotation never collides on `auth.sessions.refresh_token_hash`). **(4) Login world resolution:** (a) the world where the user has a job always wins; (b) a jobless user may pick explicitly via `world_id` — rejected with 403 if not a member; (c) jobless with exactly one active world → that world; (d) jobless across *multiple* active worlds → `{"status":"worlds","worlds":[...]}` (no session yet) and the client re-posts with `world_id`; (e) else a single manager row → its world; (f) else a world list; (g) no manager rows → 403 `ErrNoManager` (account exists, no world joined). **(5) Sessions rotate + revoke:** refresh stores only a SHA-256 hash in `auth.sessions` (with `ip inet`, device fingerprint, `expires_at`, `revoked_at`); every refresh revokes the old row and issues a new session pinned to the same manager. **(6) Registration/recovery/email verification remain OPEN (OPD-02):** `cmd/user-create` is a developer bootstrap only (one admin/importer account), never the product signup. **(7) Non-goals this sprint:** no auth events, no rate limiting (S02-04/S11), no formal CSRF policy (httpOnly + SameSite=Lax covers dev/localhost; revisit when cross-site deployments land), "last world visited" not persisted — world resolution defaults as in (4). | S02-01 |

| **OPD-16** (world lifecycle + manager→club assignment boundary, S02-02) | **(1) Worlds and clubs are created by an admin at game start:** world lifecycle is an admin-only HTTP surface (`POST /api/admin/worlds` to provision, `POST /api/admin/worlds/:id/status` to launch/pause/resume/archive). A world starts `provisioning`; `active`/`open_beta` are playable (assignments, offers, upcoming scheduler ticks), `archived` is terminal (nothing can leave it). `world.world_config` cadence keys are seeded on launch from `internal/world.defaultConfigKeys` (S02-03 reads the same keys). **(2) A first club is a JOB OFFER from an AI club — the ONLY sanctioned assignment path:** "When the game is started, new clubs are created by an admin; clubs without real user managers are managed by the system, via a native AI. When we have new users, these AI-managed clubs offer jobs to the new manager, who accepts or declines." So `manager.job_offers` (migration 0024) records `proposed → accepted|declined|expired`; offers may only originate from an AI club (`is_ai_controlled`), target an unemployed human manager in the same world, and one pending offer per (club, manager) is enforced (`uq_job_offer_pending_club_manager`). Only `cmd/user-create` (an admin account) can create an offer on an AI club's behalf today; **no auto-issue and no auto-hire on join exist**. **(3) One active assignment per manager, platform-wide:** humans are covered by OPD-15(2) (`uq_manager_one_job_per_user`); AI managers that carry a person record get `uq_manager_one_active_person` (migration 0024; partial index on `person_id` where active+club). Pure policy-bot rows have no person record and remain per-world entities (still bounded per club by `uq_manager_one_active_club`). New `CHECK (status='active' OR current_club_id IS NULL)` on `manager.managers` means resigning/sacking always clears the club. **(4) Accepting a job hands the club over:** the incumbent AI manager stands down (its row → unemployed, no club) so `uq_manager_one_active_club` has room; `club.is_ai_controlled` flips FALSE; a `manager.manager_history` row opens. Resign (self-service, actor=manager) and sack (actor=board; engine hook for S06) close that row with `end_date` + `outcome_summary` — history is **never deleted**. **(5) Events + append-only reputation on assignment borders:** `JOB_OFFER_ACCEPTED`, `MANAGER_RESIGNED/SACKED` (plus `WORLD_*` lifecycle) events are written to `world.events` inside the same transaction; a `career` category was added to `manager_reputation_events` for small data-driven deltas at these borders (`+5` accept, `-10` sack — tuning constants, not compiled-in rules). `GetReputation`/`ListCareerHistory` are global display reads; `WorldReputationTotal` is the world-scoped operational read (hiring math stays S06). **(6) OPD-01 is deferred, not replaced:** initial league composition/regional structure stay OPEN and move to S03-01 — S02-02 hardcodes no league numbers. Out of scope today: auto-offer generation (no registration — OPD-02), club/league/squad generation at game-start (S03-01), interview UI. | S02-02 |

| **OPD-17** (configurable world clock, S02-03) | **(1) The world clock is a dedicated scheduler binary driven by runtime config.** The scheduler reads each playable world's cadence strings from `world.world_config` (`tick.*_cadence`, cron 5-field specs) and fires `WORLD_TICK` events over the event bus. The spec set the clock drives is `hourly|daily|weekly|monthly|seasonal` per plan §5 — cadence *values* live in the DB, never in code; changing a value (admin `POST /api/admin/worlds/:id/config`) takes effect on the next sync, **no redeploy**. **(2) Match ticks are NOT world-cron.** `tick.match_cadence` is seeded but ignored by the world clock: plan §5 hands live match ticks to per-match goroutines in the match engine (S04). The plan's economic/social(daily-adjacent), training/daily, league/weekly, financial/monthly, and seasonal passes map to `hourly|daily|weekly|monthly|seasonal` respectively. **(3) Ticks are world-scoped and monotonic.** Only `active`/`open_beta` worlds tick; each emission advances that world's `current_tick` and is written to `world.events` with payload `{"granularity":"<g>"}` + actor `system` — subscribers filter by granularity to handle only the cadences they own. Paused worlds stop ticking; resumed worlds resume from the config. **(4) One scheduler leader.** The scheduler holds a Postgres advisory lock (`hashtext('touchline:scheduler')`) for its lifetime, so duplicate scheduler instances cannot double-fire cadences; a second instance waits for leadership. **(5) Soundness on failure.** Fire guards playable status inside the same transaction (`UPDATE … WHERE status IN ('active','open_beta')`), making stale cron entries safe after pause/archive; malformed cron specs are skipped + logged, never fatal. Match-range determinism and ledger-side effects of ticks arrive with the engines that consume them (S03-01, S04, S06). | S02-03 |

| **OPD-18** (world bootstrap, S03-01) | **(1) One admin trigger materializes a whole provisioning world.** `POST /api/admin/worlds/:id/bootstrap` turns an empty `provisioning` world into one AI starter club with a squad in a single transaction: `404` unknown world, `409` if the world is already launched or already has clubs, `503` when the `ref` pools are empty, and `CLUB_CREATED` + `WORLD_BOOTSTRAPPED` events written in the same tx. **(2) First club = AI starter club; humans enter via OPD-16 only.** The bootstrap club is `is_ai_controlled=TRUE` with a policy-bot `manager.managers` row; the only sanctioned path for a human manager to take it is the OPD-16 offer→accept flow (admin `POST /api/admin/offers`). No auto-hire, no direct assignment. **(3) Squad comes from the shared generation subsystem.** `SquadSizeDefault=24` (2 GK / 7 DEF / 7 MID / 6 FWD + 2 flexible outfield), generated by `pkg/playergen` with a collision registry; pools read from `ref.nationalities` + `ref.name_pool` at runtime per OPD-13(1). Ages 17–33; `date_of_birth` anchored to `COALESCE(launched_at, created_at)` so ages are season-consistent. **(4) Seeded for replay.** One int64 seed per world drives the whole squad and is persisted on `WORLD_BOOTSTRAPPED.random_seed`; identical seeds reproduce identical squads. **(5) Reads are world-scoped.** `GET /api/clubs` (caller's world via the manager row, OPD-15(3)) and `GET /api/clubs/:id` (club + policy-bot manager + squad; `404` across worlds) are the S03-01 read surface. Attributes/contracts/finance and squad→depth-chart ownership arrive with S05; competition structure is OPD-01 (S04-01). | S03-01 |
| **OPD-19** (Redis boundary + the single realtime socket, S02-04) | **(1) Redis is an ephemeral transport, never authoritative storage.** S02-04 uses Redis strictly for WebSocket **pub/sub fan-out** (AC1); all authoritative gameplay state stays in Postgres (`world.events` is the replay source). Sessions/rotation remain in `auth.sessions` per OPD-15(5); rate limiting stays deferred to S11/OPD-08. **(2) The browser authenticates the handshake with the existing httpOnly cookie.** `/ws` runs behind `requireAuth` (OPD-15 access token; same-site localhost works), the server checks the browser `Origin` against `APP_ORIGIN` before upgrading (cross-site socket hijack blocked), and the world is derived from the manager row (OPD-15(3)). A cross-site ticket endpoint is documented future work if deployments ever split frontend/API origins. **(3) One socket per session.** Nuxt `useSocket()` is a singleton — screens subscribe by type through Pinia stores (new `stores/realtime`), never open their own sockets (AC3). **(4) Multi-pod fan-out.** All delivery flows through a broker subscription: Redis pub/sub (single channel `touchline:realtime`; events carry `world_id`) so 1..N API pods behave identically, with an in-process `LocalBroker` fallback when `REDIS_URL` is unset/unreachable (bare `go run`; fail-soft, never fatal). Events are world-scoped at delivery — a socket can never observe another world's stream. **(5) Proof event + control protocol (AC5).** The scheduler worker publishes `world_tick {event_id, granularity, tick}` for every `WORLD_TICK`, so browsers watch fire-and-forget live ticks; client `ping`→`pong`, unknown client types answer with a typed `error` envelope, unknown server types are ignored by the client (warn + continue). A missed live push never corrupts state — replay always comes from Postgres. | S02-04 |
| **OPD-20** (competition structure — resolves OPD-01, S04-01) | **(1) Leagues are admin-declared entities per country; the engine infers nothing.** Competition shape (countries, leagues, tiers, team counts, promotion/relegation numbers) is declared up-front via the admin HTTP surface — `POST /api/admin/countries`, `POST /api/admin/leagues`, `PATCH /api/admin/leagues/:id/adjacency` — before `seed-competition` is called. This **resolves OPD-01**: no initial league-size/P&R numbers are baked anywhere; the admin's declared values are exactly what the engine consumes. **(2) Adjacency must be symmetric** (`validateAdjacency`, seed-time only): a league's `promotions` must equal its promoted-to league's `relegations`, any non-zero movement requires a link, and a league with no neighbors must declare zero movement — else `ErrAdjacencyMismatch` at seed time. Leagues may be created before neighboring leagues exist (no create-time link requirement). **(3) Reads are world-scoped and tier-ordered.** Manager reads (`GET /api/competitions`, `/:id`, `/:id/fixtures`, `/:id/standings`) resolve the caller's world from the manager row (OPD-15(3)); the world's starter club always lands in the top tier so a lone manager always has a visible competition. | S04-01 |
| **OPD-21** (live match execution, S04-02) | **Live matches run on per-match real-time goroutines (Tech Plan §5), never on the world clock.** Matches in the fixture range auto-start, tick in real time at a world-config cadence (default ~20s per simulated minute, key `tick.match_cadence` is seeded but the world clock ignores it per OPD-17(2)), accumulate live events, and finish deterministically at full time. Managers can issue **live substitution/tactic changes** that feed ordered live inputs into the running simulation (S05-01 controls; the engine replays `seed + ordered inputs` to the identical outcome). The observer feed is delivered over the S02-04 realtime socket (S04-03). | S04-02 |
| **OPD-22** (seed & rollover contract, S04-01) | **(1) One seed per country, seeded from the bootstrap.** `POST /api/admin/worlds/:id/seed-competition` builds one master `*rand.Rand` from `WORLD_BOOTSTRAPPED.random_seed` (503 `ErrWorldNotBootstrapped` otherwise); every club and fixture derives from it, so identical seeds reproduce identical competitions. A league whose fixtures already exist errors `409 ErrLeagueAlreadySeeded`; the whole country seeds in a single transaction. **(2) Entry construction.** The starter club (OPD-18) is placed in the selected league; leagues then fill to `team_count` with `GenerateAIClub` clubs using fresh distinct names; all non-starter clubs must be consumed by top tier (else `ErrNoStarterClub`). **(3) Fixture shape.** Full home-and-away double round-robin; `matchday` = round index + 1 (so a two-leg pair per opponent); `scheduled_at` anchored to the bootstrap reference, kickoff 19:00 UTC — **superseded by IM03** (fixture pacing): matchdays are now paced at `matchdays_per_week` (default 3) over `days_per_week` (default 7) with kickoffs rotating through the league's `kickoff_hours` (default `[15,18,20]` UTC); see `docs/tasks/improvements/IM03-fixture-calendar-and-match-pacing.md`. **(4) Events.** Per league `SEASON_CREATED` (season_id/label/number, team_count, fixture_count, matchdays) plus one country-wide `COMPETITION_SEEDED` with league summaries. **(5) Rollover cascade.** When the last fixture in a country completes, `maybeCompleteSeason` fires one transaction-per-country `rolloverCountry`: every league's season → `completed`, next entries = stayers + promoted-from-below + relegated-from-above (appended, never overwritten; team counts must reproduce exactly or the tx aborts with an internal error), and next seasons/fixtures schedule from the last-scheduled-day anchor. Emits `SEASON_COMPLETED` (champion), `CLUB_PROMOTED`/`CLUB_RELEGATED` (destination_id), and per-league `SEASON_CREATED` with entries. | S04-01 |
| **OPD-23** (event publication contract — resolves P0 `docs/em/event-delivery-atomicity.md`, S04-04) | **(1) The transactional outbox is the only producer path.** Every event producer records the `world.events` row and enqueues its `touchline_event` River job in the *same* transaction as the business state it describes — via the shared primitive `eventbus.RiverBus.PublishTx(ctx, tx, ev)` (`Publish` becomes `BEGIN → PublishTx → COMMIT`). A committed state change whose event is undispatched is impossible by construction; the event row, its dispatch, and the state are one atomic unit. **(2) No silent fan-out.** A failed event insert/enqueue aborts the enclosing transaction; publish errors always propagate (removes the manager `publish` discard and the competition rollover `_ = recordSeedEvent` discards). **(3) Identity captured at record time.** Producers write with `RETURNING id, occurred_at` into the same `*Event` instance so dispatch and any causal chain (`caused_by_event_id`) reference the persisted ID, and events stamp the real `world.worlds.current_tick` (competition events no longer hardcode `0`). **(4) At-least-once, idempotent, repairable, observable.** Duplicate publishes of one ID dedupe (`ON CONFLICT (id)` + River `ByArgs` unique). A worker repair sweep re-enqueues any committed-but-undispatched row by its original ID (scan for rows lacking a matching `touchline_event` job; `Publish` re-enqueue; safe with N workers) and logs `event_repair_sweep {scanned, repaired, oldest_lag_s}`; the existing river queue-depth metric drives the worker HPA. **(5) Realtime is a consumer, not a producer.** `world.events` stays authoritative and append-only; Redis remains ephemeral fan-out (OPD-19(1)); `world_tick`/`match_tick` envelopes are published by the worker *after* dispatch, not as producer outbox writes. | S04-04 |
| **OPD-24** (world calendar semantics — resolves P0 `docs/em/world-calendar-tick-semantics.md`, S04-04) | **(1) The calendar is a dedicated day counter, not the monotonic tick.** New `world.worlds.current_day` counts in-game days since launch; `worldDate` computes `date = COALESCE(launched_at, created_at) + current_day days`. `current_tick` stays the monotonic ordering/audit counter advanced by *every* granularity (OPD-17(3); form/live reads and `world.events.world_tick` stamping unchanged). **(2) Only the daily emission advances the calendar.** Each `WORLD_TICK{daily}` increments `current_day` by exactly one in the same transaction as the counter; hourly/weekly/monthly/seasonal emissions never touch it. Contract: one daily emission ⇔ one in-game day, so fixture eligibility on the daily handler advances exactly one matchday regardless of the other four schedules; paused worlds don't advance; the day count is the count of daily emissions since launch (repeated emissions simply mean days passing). **(3) Default pacing ≈ 3 in-game days per real day.** Seeded default `tick.daily_cadence` changes to `0 */8 * * *` (00/08/16 UTC); cadence stays runtime-configurable per world (OPD-17(1)) — changing it alters the *rate* the calendar advances, never single-step semantics, and never leaks into other granularities' timing. **(4) Existing worlds.** Migration backfills `current_day` from each world's stored daily `WORLD_TICK` count so already-seeded dev fixtures stay eligible; the new default cadence applies to worlds launched after deploy (config seeds `ON CONFLICT DO NOTHING`, so untouched worlds keep their old rate). | S04-04 |
| **OPD-25** (MVP Simple-Mode tactics — resolves OPD-04, S05-01) | **(1) The Simple-Mode tactical control is one of five styles.** Tactics are not formation/mentality/pressing sliders: the manager picks **`balanced` | `possession` | `gegenpress` | `low_block` | `direct`**, each per the approved 5-style specification in `docs/tasks/S05-01-squad-tactics-and-simple-training.md`. Formation is an optional refinement inside the style's allowed set (§1.1 of `docs/design/tactics-training-numerics.md`), defaulting to the style's canonical shape. **(2) Styles modulate the match engine.** Each style carries a numeric block (possession shift, chance volume, own + conceded conversion, card rate, stamina decay) consumed by `matchsim.StyleSpec` and a pre-match attribute-weight profile in the squad rating pipeline; values are the published ranges' midpoints, **proposal** pending PM tuning sign-off (data-only change). **(3) Determinism is preserved.** RNG consumption order is unchanged; styles re-weight existing draws; live in-match style switches are ordered `tactic_change` LiveInputs replayed `seed + ordered inputs` to identical outcomes. **(4) Training is a separate weekly Simple-Mode surface** (five archetypes → `player.player_attributes` key deltas + `player.player_condition`), and leaves individual training/development programmes to S08. | S05-01 |
| **OPD-26** (country academy / street kids — resolves OPD-19(2) pool inflow, A04/A05) | **(1) Streets are the country-level, manager-agnostic player intake.** New origin `street` — kids discovered **13–15**, `country_id`-anchored, `pool_origin='street'`, immediately **signable** as free agents, **match-ineligible until 18** (the A07 gate below). **(2) Intake is seasonal and country-scoped.** Each country's seasonal street intake seeds **10–20 street kids** (`StreetMinCount/StreetMaxCount`) aged **13–15** (`StreetMinAge/MaxAge`), replenishing the country pool; street intake capacity comes standard from S08-02. **(3) The education gate is the concrete A07 eligibility rule.** Street origin enforces the 18+ floor: a street-signed player cannot be selected to a first-team match squad until their **18th birthday**; academy-origin and draft-origin products have no age floor. | A04/A05 |
| **OPD-27** (player pool draft model — resolves OPD-18, A02/A03/A09) | **(1) The country pool is the shared, permanent resource.** `playerpool.player_pool` is a **country-wide** free-agent pool (one per country), always kept at a target density (`TargetSquadSize`=24 for eligibility, OPD-29). Clubs **draft** from it; free agents are an ongoing replenishment resource, never a one-time starter lump. **(2) The draft is occurrence-scoped, not the only source.** On world bootstrap/rollover, starter squads are drafted from the country pool deterministically (OPD-18); afterwards, intake (OPD-26) + academy + street replenish it. **(3) AI and humans draft from the same pool.** AI auto-fill reads the same pool with the same eligibility/quality gates; no separate AI-only player class. | A02/A03/A09 |
| **OPD-28** (match eligibility rule — resolves OPD-19, A07) | **(1) A player is match-eligible iff: professional contract signed; not on loan; not retired/suspended; and either origin != `street` OR age ≥ 18.** Street is the only origin with an age floor. **(2) Contracts are the professional gate.** Academy products without a pro contract are ineligible; signing a youth→pro contract opens eligibility. **(3) The gate is enforced at squad-selection time** (the observable read surface + squad-selection gate), never at intake. | A07 |
| **OPD-29** (squad size — resolves OPD-20, A09) | **(1) Target squad size is 24.** `TargetSquadSize=24` for AI-filled squads; academy/street youth intake does **not** count against the target (they're developed outside the first-team unit). **(2) AI auto-fill backstops thin squads:** when a club's first-team squad drops below target, `aftermath`/`AIFill` tops it up from the country pool to the same eligibility/age profile. **(3) The 24 target is a tuning constant** in `squad.DefaultSquadNumerics`; recalibration is data-only. | A09 |
| **OPD-05** (transfer workflow rules, S06-01) | **(1) One `transfer.bids` row is a negotiation thread.** Every counter appends a `transfer.negotiations` round and the latest round is always the live offer; accepting replies to the other side's proposal only. **Terms ride with the bid** — fee, weekly wage, contract length, signing bonus, release clause, sell-on %, buy-back £ — so there is no separate player-agreement mini-game (that arrives with S10-01 agents). **(2) Bid TTL = 3 world days** (`BidTTLWorldDays = 3`), enforced wall-clock on `respond` and by the daily expiry sweep; listings have **no expiry**. **(3) Valid clause structures:** `sell_on` (percentage to the selling club) and `buy_back` (fixed amount) only; instalments/performance add-ons stay schema-ready (`transfer.completed_transfers.installments` JSONB) but are not monetised in S06-01. **(4) AI clubs participate on both sides.** AI sellers resolve bids by valuation thresholds; the top-2 most-interested AI buyers (`aiInterestClubs`) bid on human listings immediately and daily, deterministically seeded, funds-guarded, never above asking. Full numbers: `docs/design/transfer-numerics.md`. | S06-01 |
| **OPD-03** (simulation formulae & tuning — transfer valuation slice, S06-01) | The transfer market's deterministic valuation formula and AI buy/sell policy numbers are standardized in `docs/design/transfer-numerics.md`, expressed as **proposal** constants in `internal/transfer` (recalibration is a data-only change, per the tuning-governance convention of `squad.DefaultPositionWeights`). The remaining OPD-03 curves — board job security (resolved at S06-02, see this row's resolved entry below), morale decay (S06-03), injury probability (S08-03) — stay open under the registry row above. | S06-01 |
| **OPD-30** (world regions & league reputation — IM06) | **(1) Regions are a hard product entity** (world → regions → countries), not a field on a cup: geography lives on the countries (`world.countries.region_id`, nullable — only the geography an admin actually assigns participates in regional cups) and a regional cup merely points at a region (`competition.competitions.region_id`, consumed by IM08). **(2) League reputation reuses the previously dormant `competition.competitions.reputation`** as a 0–100 admin-managed field driving the default continental-cup qualification band (IM08); verified engine-safe (its only consumer, `FixtureContext.LeagueTier`, is written but never read) and regression-guarded by the existing match integration tests. **(3) Position bands are the universal qualification primitive** — `competition.cup_qualification` rows (`from_position..to_position`, NULL to_position = through last place), rank order from the league's last completed season; consumed by IM07/IM09. **(4) Region and reputation mutations publish no events/news** (pure metadata, matching the declare-country precedent). | IM06 |

---

## Product Metrics & Stage Gates

### North Star Metric
- **Meaningful Manager Decisions per Active Manager per Week (MMD/AMW)**

### Key Performance Indicators (KPIs)
- **Retention:** D1, D7, D30, and D90 active manager retention cohorts.
- **Engagement:** Active human-vs-human fixtures completed, transfer negotiations conducted, social messages exchanged, and rivalries forged.
- **Universe Depth:** Average manager tenure per club, seasons completed, academy prospects graduated, and retired player career transitions.

---

## PM Handoff Checklist for Next Product Owner

1. **Review Context:** Read `OPENCODE.md`, `docs/Touchline — Persistent Multiplayer Football Manager PRD.md`, `docs/Touchline_Technical_Implementation_Plan.md`, and this file (`docs/product_manager.md`).
2. **Backlog Integrity:** Ensure every task executed in `docs/tasks/` maps to a sprint, has clear acceptance criteria, and records implementation evidence upon completion.
3. **Resolve Open Decisions:** Coordinate stakeholder sign-off on Open Decisions (OPD-02 to OPD-10) before work starts on dependent sprints.
4. **Scope Control:** Reject feature creep into Phase 1 MVP that belongs in V1 (Phase 2) or V2 (Phase 3).
5. **Verify Non-Negotiables:** Validate that all state-changing features write to `world.events`, maintain ledger integrity, return `Explanation` objects, and run server-authoritatively.
| **OPD-30** (dressing-room factions & squad dynamics — resolves S09-02, A07) | **(1) The dressing room is a graph derived at read time.** The player↔player subgraph of `social.relationships` is read with an undirected recursive CTE (`componentRoots`, root = min reachable id) and fed to a pure, race-clean engine in `internal/faction` — dressing-room **tiers** (team_leader / highly_influential / influential / other, by graph centrality) and **factions** (connected components, deterministic labels: veteran_core / foreign_cohort / youth_alliance / neutral_room) are **derived at read time**, never stored as mutable state; no denormalized faction column. **(2) The full management-action matrix resolves, not just the headline pair.** Every squad-touching action from the S09-01 catalogue passes through the engine deterministically (release / bench / drop-from-XI / wage cut / missed promise / contract approval / transfer bid query-block / training load / motivational+critical team talks) — acceptance `TestEvaluateCoversEveryAction`. **(3) Contagion is gradual + bounded, never 100, never instant.** `Contagion.Confidence` starts at 24, hardens hop-by-hop (bump 10 / decay 2), caps at 84, NEVER 100 — escalating at ≥60 to a board-meeting delegation and ≥80 to en-masse transfer requests, always with an `Explanation`; a sale runs the hook **before** the ownership flip so the pre-sale room is measured. **(4) The graph is generated deterministically.** `GenerateEdges` seeds each pair's affinity from `PairStream(world_seed, pair)` (national_team / academy / mentorship / friendship / rivalry) and `FormerTeammateEdges` records sale bonds; upserts are idempotent and canonical (`a<b`). Numerics in `backend/docs/design/squad-dynamics-numerics.md`; `GET /api/clubs/:id/dynamics` serves the ownership-gated read model. | A07 |
| **OPD-31** (dressing-room read — IM12, resolves the reserved slot) | **(1) The dynamics read returns influencers only.** The `other` tier tail is dropped server-side — the wire is exactly `team_leader` / `highly_influential` / `influential`, matching the frontend influencers panel. **(2) Full profile rides with each tier.** Every returned tier embeds the complete player read-model (`PlayerProfile`: identity, uniform, personality, six attribute-category means, position-weighted overall capped 99) so one ownership-gated request draws the whole panel with no per-player follow-ups. **(3) The write path is batch + no-op-guarded.** Recipient loops became single unnest inserts (parallel from/to/kinds/strengths/trusts arrays) with one stale-edge delete; a `social.squad_graph_state` FNV-1a **member hash** (seed + generation inputs only — name/uniform/attributes/loyalty/role excluded) skips the ~250k-row upsert entirely when the generation inputs are unchanged; a miss regenerates deterministically and re-stamps the hash in the same transaction. Reads were already covered by migration 0047's partial indexes — no new edge index; the fingerprint table's PK is the only new index. The 6 attribute means + squad role are aggregated in a single roster pass. | IM12 |
| **OPD-32** (domestic cup + golden goal — IM04) | **(1) Cups are admin-declared knockout competitions over a country's league clubs.** `POST /api/admin/cups` declares name + two staging variables: **N** (`first_tier_bye`) top tier-1 clubs sit out the qualifiers and **X** (`survivor_threshold`) is the survivor count at which they join, yielding a field of X+N. Validation (`422`): N≥0, X≥1, X+N≥2, and the ladder must produce even ties every round with a two-club final; if the bottom pool already equals X the top N enter immediately, if it is smaller the cup refuses to start. `POST /api/admin/worlds/:id/countries/:countryID/cups/:cupID/campaign` starts one `competition.seasons` row (`in_progress`), writes `role='cup'` memberships (capped **3 per club**, `ErrCupLimit`) + entries, and draws Round 1. A live campaign cannot be restarted (`409`). **(2) The bracket is deterministic and lazy.** Every draw is a pure function of `world_seed ⊕ cup_id ⊕ round` over the canonically-sorted advancing set, so identical seeds + results reproduce identical brackets; later rounds materialize only when the last tie of a round completes, inside the result transaction. **(3) Drawn ties resolve by golden goal.** A knockout tie level after 90' continues deterministically minute-by-minute and the first goal wins (minute >90, seed-stable); no penalties, no dedicated ET periods, regulation league results untouched. **Result application dispatches on `competition_rules.format`:** knockout never writes `competition.standings`, never triggers the promotion/relegation rollover; winners → `qualified`, losers → `eliminated`, final winner → `champion` + season `completed`. Ties are single-legged (two-legged aggregate and live-ET streaming are later tasks). | IM04 |
| **OPD-33** (weekday-aware scheduling — IM05) | **(1) Weekdays, not just day spacing.** `allowed_weekdays` (ISO `1`..`7` JSONB array) sits under `competition_rules.scheduling_rules->'allowed_weekdays'` per league/cup and under `world.countries.default_scheduling_rules->'allowed_weekdays'` as a country default; resolution is **per-competition → country → legacy IM03 day formula**, so an unconfigured season reproduces the old calendar byte-for-byte and clearing a set restores the fallback. **(2) Two structural rests, non-configurable.** A club never plays closer than **2 game-days apart** across all competitions, and a cup tie never lands on a day any country league plays while a free day fits. **(3) Cups anchor to the league calendar.** The final = first allowed weekday ≥**3 game-days after the country's latest league fixture**; earlier rounds walk backward on a seeded 2-3-day gap (`world_seed ⊕ cup_id ⊕ round`, 3-biased toward the final), snapped to a league-free day honoring the cup's own weekday set (an allowed weekday wins only if a fit exists — anchoring beats never playing; a packed window degrades to the best clearance; cross-league nudging is **not** done). Round dates are stamped on the campaign ladder and honored at materialization. **(4) League re-pacing is freeze + slide.** `PATCH /admin/leagues/:id/scheduling` with a non-empty set freezes matchdays with `live`/`completed` fixtures and slides unstarted ones strictly **forward** onto the new weekdays (order + kickoff-hour rotation preserved); clearing never moves fixtures. A country-default change re-paces every league of that country with no override. **(5) Scheduling news is country-scoped.** `world.news_stories.country_id` (NULL = world-wide) scopes a category `scheduling` story written transactionally with the cup materialization or the league re-pacing it describes; the manager feed shows world-wide + own-country stories. **Recorded for later:** F==now widened gap seams, per-club conflict resolution beyond the two-day floor, weekday-aware re-materialization of live cup dates. | IM05 |
| **OPD-34** (competition prize pools & fair play awards — IMP-01) | **(1) Domestic League Prize Pool:** Total prize pool $P_{\text{total}}$ is calculated deterministically at season launch from base tier value, mean player attributes, facility score, and league reputation: $P_{\text{total}} = P_{\text{base}}(\text{Tier}) \times (1 + 0.5 \cdot \text{PlayerQuality} + 0.25 \cdot \text{FacilityScore} + 0.25 \cdot \text{LeagueReputation})$. **(2) Merit Distribution:** Merit pool ($90\%$) is distributed among final standings using power-law quota $Q(k) = (N-k+1)^\gamma / \sum j^\gamma$ ($\gamma=1.5$ for Tier 1 top-heavy rewards, $\gamma=1.0$ for lower tiers). **(3) Custom Competitions:** Declared prize pool set during creation (`competition.competitions.declared_prize_pool`); Fair Play toggle enabled by default allocating $5\%$ of the pool to Fair Play. **(4) Fair Play Award & Calibration:** Fair Play Points ($FPP$) track yellow cards (+1), second yellows (+3), straight reds (+5), and manager warnings (+3). Lowest $FPP$ wins the Fair Play Award, calibrated equal to the average merit prize of the bottom 5 clubs ($\frac{1}{5} \sum_{k=N-4}^{N} \text{MeritPrize}(k)$). Tie-breakers: (1) Fewest straight reds, (2) Fewest yellows, (3) Higher league finish, (4) Seeded random draw; cash split equally only if all 4 tie-breakers match. **(5) Disciplinary Fine:** Teams exceeding $2\times$ league average $FPP$ incur a $5\%$ merit prize fine credited to the Fair Play pool. **(6) Financial Ledger & Board Budget:** Payouts append immutable credit/debit rows to `finance.ledger_entries` (`'COMPETITION_PRIZE_MERIT'`, `'COMPETITION_PRIZE_FAIR_PLAY'`, `'DISCIPLINARY_FINE'`). At season rollover, board policy determines budget reinvestment percentage $\alpha$ (70% standard, 90% ambitious, 20% debt-burdened). **(7) Product UX Enhancements:** Winning Fair Play grants $+3\%$ Board Confidence (with `Explanation`), a silver "Fair Play Winner" badge on club/manager profile UI, and an automated media story published by `S09-03`. Full specification: `docs/tasks/improvements/competition-prize-pools-and-fairplay.md`. | IMP-01 |

| **OPD-35** (regional / continental cups — IM08) | **(1) A regional cup is an admin-seeded `continental` knockout competition scoped to a region, `country_id NULL`.** `POST /api/admin/cups` accepts both scopes side by side — country scope keeps the IM04 staging contract unchanged; region scope carries a region id, an optional **soft tier**, and per-league qualification bands. The region is purely geographic: only countries actually assigned to the region participate, and a banded league outside it is rejected (`ErrRegionMismatch`). **(2) Qualification is one universal mechanism: per-league position bands.** Region cups qualify from `competition.cup_qualification` rows (IM06 primitive) over each league's last **completed** season, computed by the IM07 qualification engine (`ComputeField` renders origins `position` / `champion_direct` / `champion_out_of_band` / `champion_next_best`); preview (`POST /api/admin/cups/preview`) projects the field + warnings without writing. The **soft tier is a label** (+ IM09 precedence comparison), never a qualification filter or scheduling constraint. **(3) Qualification is always historical and strict.** Every banded league must have a completed season or the campaign refuses to start (`ErrQualificationUnavailable` 422); the wizard's reputation-derived default bands (rep ≥90→`1..4`, ≥75→`1..3`, ≥60→`1..2`, else `1..1`) are editing suggestions only — the engine reads the committed rows. **(4) The reigning champion is entitled, and its cascade is cap-exempt.** The champion and its next-best cascade entrant always enter (direct / in-band next-best / out-of-band) regardless of the 3-cup membership cap; positional entrants stay capped (`ErrCupLimit`). **(5) Regional rounds anchor to the union of the participating countries' league days.** The campaign calendar uses the IM05 anchored walk over the union of the banded countries' league days (final ≥3 game-days after the latest league end, weekly fallback when nothing is configured), and Round 1 materializes in the same transaction as the season, memberships, and entries. Campaign lifecycle mirrors domestic cups (one at a time, `409` double-start, `404/409/422` surface). Manager *choice* for a club drawn by multiple cups is IM09. | IM08 |
| **OPD-36** (season #1 kickoff date + launch news — IM11) | **(1) The admin owns when season #1 kicks off.** An empty start-season body anchors the calendar to the world's **current date** — matchday 1 lands the next day, so a freshly seeded world is immediately playable (replacing the old reference-date anchor). A JSON body `{"kickoff_date": "YYYY-MM-DD"}` pins matchday 1 to exactly that date; the whole deterministic calendar re-stamps off anchor = kickoff − 1 (IM03 day formula and IM05 weekday pacing unbent). Never reads the wall clock. **(2) Rejection is write-free and symmetric (422):** a kickoff date at/before the world's current date (`ErrKickoffDateInPast`) or — when a league's effective `allowed_weekdays` resolves — off an allowed weekday (`ErrKickoffNotAllowedWeekday`) fails before any write; a malformed date is 400. A pinned date skips weekday snapping (the date stands); only a genuinely non-allowed weekday is refused. **(3) Launch is announced world-wide in the season transaction.** Two country-scoped `announcement` news stories (fixtures schedule + official kickoff day) ride the same tx and link the `SEASON_CREATED` event — feeds never advertise a calendar the engine didn't keep. Back-compat: `Service.StartSeason` call sites and empty-body HTTP requests are unchanged. | IM11 |
| **OPD-37** (qualification resolution for double-qualified champions — IM09) | **(1) A champion always plays exactly one cup, decided by the campaign-start sweep.** A club that holds a seat in more than one continental cup (always the reigning champion, effectively — its `champion_*` seat plus a band seat elsewhere) is resolved at campaign start to exactly one; every club ends up in at most one cup, never silently dropped. **(2) Strict tier precedence first.** A higher-tier cup always claims the club (higher tier *integer* wins — tier 3 beats tier 1); no choice can override a strict tier difference, and the forfeited cup's freed slot cascades to its league's next-best club past the band cut. **(3) Managers choose among equal tiers.** `POST /clubs/:id/cup-choices` adds the club's opt-in (`competition.manager_cup_choices`, unique per club×cup, upsert); among equal-tier cups the choice decides. A choice for a cup the club is not projected for is rejected 422; equal-tier silence defaults to **defend the cup it is reigning champion of**, ties to the **earlier-created** cup. **(4) Cascades reach a fixpoint, then invariants hold or the start fails.** Promotions may themselves be double-qualified, so the sweep loops to stability; a champion left in zero cups, a cup under 2 entrants, or any club still in two cups is `ErrResolutionImpossible` at start — never a silent drop. The next-best cascade entrant (`origin: cascade_replacement`) is **cap-exempt** like a champion; positional entrants keep the 3-cup `ErrCupLimit`. **(5) Commitment news is transactional and start-scoped.** Each cascade touching the starting cup publishes one `world.news_stories` row (category `general`, world feed) tied to the cup's `CUP_CAMPAIGN_STARTED` event, committed with the memberships so feed and schedule never diverge; cascades for other cups publish when those cups start. Manager surfacing: `GET /clubs/:id/cup-qualifications` (projected entries, conflicts, recorded choice, next-best) + the choice endpoint; **POST-time choice-confirmation news is deferred** (frontend seam). | IM09 |
| **OPD-38** (cup final-date policy — IM10) | **(1) A cup's final is a declared policy, not just a derived constant.** `final_date_mode` on `competition.competitions`: **`calculated`** (default) re-derives the final each season — the first allowed weekday at least `final_offset_days` game-days (**default 3**) after the scope's latest league fixture (domestic: the owning country's league days; regional: the union over the region's countries); **`fixed`** pins one absolute `final_date` that every season plays exactly, never recalculated. An unconfigured cup reproduces the IM05 anchor byte-for-byte, so upgrading a world changes nothing. **(2) Creation validates once, for both scopes.** `fixed` requires a parseable `final_date` (422 otherwise) and ignores offsets, storing the default 3; `calculated` rejects a negative offset (422 — explicit `0` is valid: the final lands the first allowed weekday at/after the last league fixture) and ignores any supplied date. **(3) A live campaign re-stamps without ever moving history.** `PATCH /api/admin/cups/:id/final-date` walks the persisted ladder **backward from the new final date** over not-yet-materialized rounds; rounds at or below the last-materialized frontier stay frozen. Any edit landing at/before the latest scheduled round, or once the final round has its own fixtures, is rejected (`ErrCupFinalDateLocked`, 422). For a calculated cup an explicit `final_date` is a **per-campaign override** (declaration untouched), `null` clears it back to the computed anchor, and an offset edit re-derives future seasons; a fixed cup accepts only a date edit — offset edits and null dates are 422. **(4) The ladder is the single source of truth for cup dates.** Re-stamps persist via `jsonb_set(qualification_rules, '{campaign,ladder}')` and never touch `match.fixtures`; a date-moving edit publishes a `scheduling` news story in the same transaction so feed and schedule never diverge. Back-compat: legacy `CreateCup` call sites and unconfigured cups are behavior-identical. | IM10 |
| **OPD-39** (server-side logout + one session per account — IM13) | **(1) A user account holds ONE live session at any time.** `auth.sessions` gains a partial unique index `uq_sessions_one_live_per_user` (on `user_id` where `revoked_at IS NULL`), and every login **hard-deletes** the account's prior live session in the same transaction (before inserting the new one, so the index is never violated). A logging-in-elewhere kicks the previous device: its refresh token can never mint a new pair. **(2) Sessions are invalidated AND deleted.** Login and logout `DELETE` rows outright; only refresh *rotation* keeps the existing `revoked_at` tombstone for audit. **(3) Logout is server-side, unauthenticated, and idempotent.** `POST /api/auth/logout` deletes the row keyed by the hashed `refresh_token` cookie and clears both auth cookies regardless of whether a session matched — logout always works, even once the access token has already expired. **(4) Access tokens remain stateless with a documented grace window.** `requireAuth` stays DB-free (OPD-15(5) consistent): the already-issued access JWT authenticates until its own ≤15m TTL after logout or a kick, but can never refresh again. **(5) Enforced in the DB, not just by call discipline.** The partial index guarantees the invariant against future write paths and concurrent logins (verified by a `23505` test for a raw second live insert). Frontend (logout button / store reset) is a follow-up. | IM13 |
| **OPD-40** (league membership add + capacity resize — IM14) | **(1) Two admin controls, both binding at the NEXT season; a live season is never edited under teams.** `POST /api/admin/worlds/:id/countries/:countryID/clubs/:clubID/league` adds a **league-less** club to a league (its `country` text is normalised, its admission recorded, `CLUB_JOINED_LEAGUE` event in the same tx); it composes the league's very first season if none has ever played. `PATCH /api/admin/leagues/:id/capacity` raises `team_count` and/or restates `promotions`/`relegations`. **(2) Admission is structurally full-proof.** `role='league'` stays one-per-club-for-life (`uq_club_one_league`), so a running pyramid cannot be over-subscribed: add is refused (`ErrLeagueFull`) unless `realSize(live season entries) + pendingDeclared + 1 ≤ team_count`, and `team_count` only ever **rises** (`ErrLeagueShrink`; shrinking a composed pyramid is incoherent — stayers + movers already reproduce the old count). **(3) Adjacency symmetry is auto-maintained, error-free.** A capacity change auto-adjusts the neighbour's reciprocal count in the same transaction (the tier above's `relegations` tracks your `promotions`, the tier below's `promotions` tracks your `relegations`), then re-validates the whole country ladder (`validateAdjacency`) before committing; a strict-refusal design was rejected because treating pairs as independently declared invariants deadlocks. **(4) Composition = standings, then declared, then auto-fill.** The rollover composes each next table as stayers + movers, merges any declared members (they sort ahead), then tops up to `team_count` — first from the country's league-less pool (by name), then from seeded, deterministically-generated AI clubs — so a fixture list never comes out holey; counts and capacity are read at rollover, so a same-season change lands on the very next table. **(5) Odd counts are a 400, not a panic.** `team_count` is even-and-≥4 at the IL boundary, and `StartSeason` now refuses an odd live member count (`ErrOddMemberCount`) instead of reaching `roundRobin`. Any newly leagued club is placement-consistent: its stale home row can never be re-admitted because the rollover excludes every already-seated club. | IM14 |
